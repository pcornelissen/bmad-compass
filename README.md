# BMAD Compass

Local web dashboard that visualizes the progress of a BMAD-Method project.

## Usage

```bash
cd /path/to/your/bmad-project
npx bmad-compass
```

Opens `http://localhost:<port>` in your browser.
